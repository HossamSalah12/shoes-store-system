# The desktop client only ever needs the PUBLIC base URL of the backend API.
# This is NOT a secret — it's the same URL a browser could hit. No database
# credentials, JWT secrets, or any other server-side configuration ever
# belongs in this file or anywhere in apps/desktop.
VITE_API_BASE_URL=http://localhost:4000
